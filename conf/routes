# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~


GET /branch controllers.BankTestController.getBranchDetails()

GET /branches controllers.BankTestController.getAllBranches()
